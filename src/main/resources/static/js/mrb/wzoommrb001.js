function zoom(chamada){
    switch (chamada) {
        case "codapl": const query = ""
                       const table = zoom_init("codapl", columnGrid, columnLabel, columnWidth, columnAlign, {});
                       
                       break;
    }

    window.open("/zoom", "zoom", "_blank,width=750,height=550");
} 